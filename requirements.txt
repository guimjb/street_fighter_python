# Core runtime
kivy==2.2.1
Pillow

# Windows-only Kivy dependencies
kivy_deps.sdl2; platform_system == "Windows"
kivy_deps.glew; platform_system == "Windows"
kivy_deps.angle; platform_system == "Windows"
